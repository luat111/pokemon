import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    statItem: {
        fontSize: 18,
        color: 'black',
        fontWeight: 'bold',
        paddingBottom: 5
    },
}));
